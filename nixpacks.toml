providers = []

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["echo 'deps install deferred to build'"]

[phases.build]
cmds = [
  "cd backend && npm ci --omit=dev",
  "cd frontend && npm install --include=dev && npx vite build"
]

[start]
cmds = ["cd backend && npm start"]
