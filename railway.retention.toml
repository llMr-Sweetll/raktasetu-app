# Cron-only Railway config for raktasetu-retention.
# Point this service's Config-as-Code path to /railway.retention.toml
# so root railway.toml startCommand does not start the API.

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm --prefix backend run retention"
restartPolicyType = "never"
