{
  "name": "raktasetu-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "test:e2e:full": "npm --prefix frontend run test:e2e:full"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
