# Backend origin only — no trailing slash, no /api suffix.
# Leave unset (or empty) for same-origin Railway deploy (Express serves the SPA).
# Local Vite without proxy: http://localhost:3001
# VITE_API_URL=http://localhost:3001
VITE_APP_VERSION=1.0.3
